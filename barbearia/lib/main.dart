import 'package:flutter/material.dart';

void main() => runApp(const MyBarberShop());

class MyBarberShop extends StatelessWidget {
  const MyBarberShop({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(

      

    );
  }
}